function addItems(n) { // O(1) Is the most efficient operation. it doesn't matter how many operations run it simplifies to O(1)
    return n + n // considered 1 operation n + n + n two operation etcetera.
}
